task: tools
tool: sample
type: hydra
input_path: /data/korepanov/datasets/Hydra.DS/HYDRA-Specim/v2/split
output_path: /data/korepanov/datasets/Hydra.DS/HYDRA-Specim/v2/dataset
split:
  train:
    source: ${input_path}/train/source
    target: ${input_path}/train/target
  val: 
    source: ${input_path}/test/source
    target: ${input_path}/test/target
params:
  crop_size:  64
  n_crops: 20
seed: 43
